export const analysisService = {
    // Placeholder for actual API calls or business logic
    analyzeImage: (imageData) => {
      return new Promise((resolve) => {
        setTimeout(() => resolve("Analysis result here"), 2000);
      });
    },
  };
  
